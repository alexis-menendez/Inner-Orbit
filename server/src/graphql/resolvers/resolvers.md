# resolvers
This folder contains the resolver functions for your GraphQL schema.

Each resolver maps a query or mutation to actual backend logic, including:
- Database calls
- Auth checks
- Response formatting
